<%= simple_form_for [@user, @event] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :start_time %>
    <%= hidden_field :agreed, true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
